# === Python ===
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# === Virtual environments ===
.venv/
venv/
env/
ENV/

# === IDE ===
.vscode/settings.json
.idea/
*.swp
*.swo
.DS_Store
Thumbs.db

# === Jupyter ===
.ipynb_checkpoints/
*.ipynb

# === Kedro data layers (commit only .gitkeep) ===
data/01_raw/*
data/02_intermediate/*
data/03_primary/*
data/04_feature/*
data/05_model_input/*
data/06_models/*
data/07_model_output/*
data/08_reporting/*
!data/**/.gitkeep
!data/**/sample_*.csv
!data/**/sample_*.parquet

# === MLflow artefacts ===
mlruns/
mlartifacts/

# === Feast feature store (registry/online store/materialized parquet are
# regenerated by `make feast-publish`; only the .py definitions are tracked) ===
feature_store/data/
mlflow.db

# === kedro-viz dataset stats hook output ===
.viz/

# === Credentials & secrets ===
conf/local/credentials.yml
.env
.env.*
*.pem
*.key

# === Great Expectations ===
great_expectations/uncommitted/

# === Docker ===
.docker/

# === LaTeX intermediates (only report.tex and report.pdf are committed) ===
report/*.aux
report/*.toc
report/*.out
report/*.log
report/*.fls
report/*.fdb_latexmk
report/*.synctex.gz

# === OS ===
.DS_Store
.AppleDouble
.LSOverride
Desktop.ini
$RECYCLE.BIN/

# === Logs ===
*.log
logs/

# === Coverage ===
.coverage
htmlcov/
.pytest_cache/

# === NOTE: .claude/ is intentionally NOT ignored (shared team config) ===
# Personal overrides go in .claude/local/ which IS ignored:
.claude/local/
